@model ICollection<Kutuphane.Web.Models.KitapModel>

<tr>
    <td colspan="8" style="padding:20px 10px;"><h2 style="color:black">Bazı Kitaplar</h2></td>
</tr>

<tr>
    @for (int i = 0; i < Model.Count; i++) {
    <td style="padding:5px 10px;">
        <img src="@Model.ElementAt(i).imgurl" style="height:200px;">
        <h4>@Model.ElementAt(i).head</h4>
    </td>
    }
</tr>
